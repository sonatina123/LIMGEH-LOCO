/// <reference types="node" />
import * as canvas from "canvas";
export declare const getCanvasImage: (options: {
    buffer?: Buffer;
    base64?: string;
    url?: string;
    arrayBuffer?: ReadonlyArray<number> | Uint8Array | ArrayBuffer;
}) => Promise<canvas.Image>;
