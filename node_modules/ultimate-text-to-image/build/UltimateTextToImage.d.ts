import { BaseClass } from "./BaseClass";
import { Measurable } from "./Measurable";
import { IMeasuredParagraph, IOptions, IRenderOptions } from "./types";
export declare class UltimateTextToImage extends BaseClass {
    text: string;
    options: Partial<IOptions>;
    renderOptions: Partial<IRenderOptions>;
    static measurable: Measurable;
    static maxSize: number;
    private _measuredParagraph?;
    private _defaultOptions;
    constructor(text: string, options?: Partial<IOptions>, renderOptions?: Partial<IRenderOptions>);
    get measuredParagraph(): IMeasuredParagraph;
    render(): this;
    private _createCanvas;
}
